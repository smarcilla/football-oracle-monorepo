echo "Running pre-push checks..."

# Unit tests - TODO: Enable when tests are available
#echo "Running tests..."
#pnpm test -- --run || exit 1

# Build
echo "Building..."
pnpm build || exit 1

echo "Pre-push checks passed!"
